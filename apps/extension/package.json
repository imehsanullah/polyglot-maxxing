{
  "name": "@polyglot-maxxing/extension",
  "version": "0.1.0",
  "description": "An open-source, local-first browser extension for dual subtitles, contextual explanations, and vocabulary learning.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wxt",
    "build": "wxt build",
    "compile": "wxt prepare && tsc --noEmit",
    "test": "wxt prepare && vitest run"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.7",
    "happy-dom": "^20.11.6",
    "typescript": "^5.9.3",
    "vitest": "^4.1.11",
    "wxt": "^0.21.4"
  }
}
